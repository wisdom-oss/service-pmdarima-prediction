services:
  water-demand-prediction_name:
    image: image_water-demand-prediction:0.6
    build: .
    container_name: service-water-demand-prediction
    ports:
      - "8080:8080"
    networks:
      - flask-network
networks:
  flask-network:
    driver: bridge
